# Dependencies
node_modules/
vendor/

# Build output (generated by `npm run build`)
theme/dist/
theme/**/*.map

# Vite / dev
.vite/
*.log
npm-debug.log*

# ACF field-group JSON is COMMITTED (source of truth) — do not ignore acf-json/

# WordPress secrets / env (never commit)
wp-config.php
.env
.env.*
*.sql
*.sql.gz

# Local-only reference (extracted proprietary code — never push)
reference/

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# Local by Flywheel / WP install artifacts (in case repo is ever placed inside wp-content)
uploads/
wp-content/uploads/
_quarantine-bluehost/
